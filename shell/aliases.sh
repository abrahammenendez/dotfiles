alias gadda="git add -A"
alias gcheck="git checkout"
alias gcheckb="git checkout -b"
alias gdiff="dot git pretty-diff"
alias gstatus="git status -sb"
alias gfetch="git fetch --all -p"
alias gpush="git push"
alias gpushf="git push --force"
alias gpull="git pull --rebase --autostash"
alias gbranch="git branch"
alias glog="dot git pretty-log"
alias gc="git commit -m"
alias glast="log -1 HEAD"

alias ..="cd .."
alias ...="cd ../.."
alias ll="exa -l"
alias la="exa -la"
alias tt="exa --tree --level=2 --long"

# Jumps
alias ~="cd ~"
alias code='cd ~/dev/abrahammenendez/code'
alias envi="cd ~/dev/env/"
alias dotfiles="cd ~/dev/env/.dotfiles"

# Linux
alias install="sudo apt-get install"
alias remove="sudo apt-get remove"

# Brew
alias brwe="brew"
alias bsl="brew services list"

# Others
alias ping='prettyping --nolegend'
alias k='kill -9'

